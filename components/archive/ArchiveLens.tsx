/**
 * Section 5: Archive Lens.
 * Reserved slot for the forthcoming lens feature.
 * Returns null until Section 5 is built.
 */
export default function ArchiveLens({
  entryId: _entryId,
}: {
  entryId: string;
}) {
  return null;
}
